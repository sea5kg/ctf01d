services:
  service2_py:
    build: .
    image: service2_py:latest
    volumes:
      - "./flags:/root/flags"
    ports:
      - "4102:4102"
    restart: always
    networks:
      - service2_py_net

networks:
  service2_py_net:
    driver: bridge
        
